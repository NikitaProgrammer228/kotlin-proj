# Почему сенсор может работать на 200 Гц, но по умолчанию даёт только 10 Гц?

## 📊 Что говорит спецификация

**Из datasheet сенсора WT901BLE:**
- **Output rate (частота вывода данных):**
  - Минимум: 0.2 Гц
  - **По умолчанию: 10 Гц** ⚠️
  - **Максимум: 200 Гц** ✅

**Вывод:** Сенсор **может** работать на 200 Гц, но **по умолчанию** настроен на 10 Гц.

---

## 🔍 Почему SDK использует 10 Гц по умолчанию?

### Причина 1: Универсальность и совместимость

**SDK от производителя настроен на "безопасные" значения:**
- ✅ Работает на любом устройстве
- ✅ Не перегружает BLE канал
- ✅ Экономит батарею
- ✅ Совместим со старыми версиями Android

**10 Гц — это "безопасный" выбор**, который гарантированно работает везде.

### Причина 2: Перегрузка BLE канала

**По умолчанию сенсор отправляет ВСЁ:**
- Ускорения (AccX, AccY, AccZ)
- Угловые скорости (GyroX, GyroY, GyroZ)
- Углы (AngleX, AngleY, AngleZ)
- Магнитометр (MagX, MagY, MagZ)
- Батарея, температура и т.д.

**Размер пакета:** ~20-30 байт на каждый сэмпл

**При 200 Гц:**
- 200 пакетов/сек × 25 байт = **5000 байт/сек = 40 кбит/сек**
- BLE канал имеет ограниченную пропускную способность
- При такой нагрузке пакеты теряются, и реальная частота падает до 10-20 Гц

**При 10 Гц:**
- 10 пакетов/сек × 25 байт = **250 байт/сек = 2 кбит/сек**
- BLE канал легко справляется
- Стабильная работа

---

## ⚙️ Что нужно сделать для получения 200 Гц?

### Шаг 1: Настроить регистр RRATE (Return Rate)

**Регистр RRATE (0x03) контролирует частоту вывода:**

| Значение | Частота |
|----------|---------|
| 0x06 | 10 Гц (по умолчанию) |
| 0x07 | 20 Гц |
| 0x08 | 50 Гц ✅ (текущая настройка) |
| 0x09 | 100 Гц |
| 0x0A | 200 Гц ⚠️ |

**Команда для установки 200 Гц:**
```
FF AA 03 0A 00
```

### Шаг 2: Настроить регистр RSW (Return Content)

**КРИТИЧЕСКИ ВАЖНО:** Для 200 Гц нужно отправлять **МИНИМУМ данных**.

**Регистр RSW (0x02) контролирует, что возвращается:**

| Значение | Данные | Размер пакета |
|----------|--------|---------------|
| 0x1E | Всё (ACC+GYRO+ANGLE+MAG) | ~25 байт |
| 0x02 | Только ускорения | ~6 байт ✅ |
| 0x08 | Только углы | ~6 байт |
| 0x0A | Ускорения + углы | ~12 байт |

**Для 200 Гц нужно:**
- RSW = 0x02 (только ускорения) или 0x08 (только углы)
- При RSW = 0x1E (всё) 200 Гц **невозможны** из-за перегрузки канала

### Шаг 3: Использовать проводное подключение (UART)

**200 Гц возможны ТОЛЬКО через UART (проводное подключение):**
- UART: 115200 bps (достаточно для 200 Гц)
- BLE: ограниченная пропускная способность

**BLE ограничения:**
- Connection interval: минимум 7.5 мс (максимум ~133 Гц теоретически)
- Реально: ~50-100 Гц при оптимальных условиях
- 200 Гц по BLE **практически невозможны**

---

## 🎯 Почему мы используем 50 Гц вместо 200 Гц?

### Технические ограничения BLE

**BLE (Bluetooth Low Energy) имеет физические ограничения:**

1. **Connection Interval:**
   - Минимум: 7.5 мс (теоретически 133 Гц)
   - Реально: 15-20 мс (50-66 Гц)
   - С `requestConnectionPriority(HIGH)`: ~15 мс (50-66 Гц)

2. **Пропускная способность:**
   - BLE 4.0: ~1 Мбит/сек (теоретически)
   - Реально: ~50-100 кбит/сек для данных
   - При 200 Гц и 25 байт/пакет: 40 кбит/сек (на грани)

3. **Потери пакетов:**
   - При перегрузке пакеты теряются
   - Реальная частота падает хаотично

### Практические результаты

**Наши тесты:**
- **10 Гц (по умолчанию):** Стабильно, но медленно
- **50 Гц (RSW=ACC_ONLY, RRATE=0x08):** ✅ Стабильно, оптимально для BLE
- **100 Гц (RRATE=0x09):** ⚠️ Нестабильно, пакеты теряются
- **200 Гц (RRATE=0x0A):** ❌ Невозможно по BLE

---

## 📋 Сравнение: 10 Гц vs 50 Гц vs 200 Гц

| Параметр | 10 Гц (по умолчанию) | 50 Гц (наша настройка) | 200 Гц (теоретически) |
|----------|---------------------|----------------------|----------------------|
| **RSW** | 0x1E (всё) | 0x02 (ACC_ONLY) | 0x02 (ACC_ONLY) |
| **RRATE** | 0x06 | 0x08 | 0x0A |
| **Размер пакета** | ~25 байт | ~6 байт | ~6 байт |
| **Пропускная способность** | 2 кбит/сек | 2.4 кбит/сек | 9.6 кбит/сек |
| **BLE Connection Interval** | 100 мс | 15-20 мс | 5 мс (невозможно) |
| **Стабильность** | ✅ Отлично | ✅ Отлично | ❌ Невозможно |
| **Подключение** | BLE | BLE | Только UART |

---

## 🔧 Что мы сделали для получения 50 Гц

### Изменения в коде:

1. **Настроили RSW = 0x02 (ACC_ONLY):**
   ```kotlin
   sensor.sendProtocolData(byteArrayOf(
       0xFF.toByte(), 0xAA.toByte(), 
       0x02,  // RSW register
       0x02,  // RSW_ACC only
       0x00
   ))
   ```

2. **Настроили RRATE = 0x08 (50 Гц):**
   ```kotlin
   sensor.sendProtocolData(byteArrayOf(
       0xFF.toByte(), 0xAA.toByte(), 
       0x03,  // RRATE register
       0x08,  // 50 Hz
       0x00
   ))
   ```

3. **Добавили `requestConnectionPriority(HIGH)`:**
   - Уменьшает connection interval с 100 мс до 15-20 мс
   - Увеличивает пропускную способность BLE

### Результат:
- ✅ **50 Гц** стабильно (вместо 10 Гц)
- ✅ Увеличение в **5 раз**
- ✅ Оптимально для BLE

---

## 💡 Почему не 200 Гц?

### Ограничения BLE:

1. **Connection Interval:**
   - Минимум: 7.5 мс (теоретически)
   - Реально: 15-20 мс даже с HIGH priority
   - Для 200 Гц нужно: 5 мс (невозможно)

2. **Пропускная способность:**
   - BLE канал ограничен
   - При 200 Гц пакеты теряются
   - Реальная частота падает до 10-20 Гц

3. **Android BLE Stack:**
   - Оптимизирован для энергосбережения
   - Не поддерживает такие высокие частоты

### Решение для 200 Гц:

**Использовать UART (проводное подключение):**
- UART: 115200 bps (достаточно для 200 Гц)
- Нет ограничений connection interval
- Стабильная работа на 200 Гц

**Но:** Это требует USB/UART подключения, а не BLE.

---

## 🎯 Итоговый ответ

### Почему по умолчанию 10 Гц?

1. **SDK настроен на "безопасные" значения** — работает везде
2. **По умолчанию отправляется всё** (ACC+GYRO+ANGLE+MAG) — перегружает канал
3. **BLE connection interval** по умолчанию ~100 мс — ограничивает частоту

### Почему мы получили 50 Гц?

1. ✅ Настроили **RSW = ACC_ONLY** (уменьшили размер пакета)
2. ✅ Настроили **RRATE = 50 Гц** (увеличили частоту)
3. ✅ Добавили **requestConnectionPriority(HIGH)** (уменьшили connection interval)

### Почему не 200 Гц?

1. ❌ **BLE connection interval** не может быть меньше 15 мс
2. ❌ **Пропускная способность BLE** ограничена
3. ❌ **Android BLE stack** не поддерживает такие высокие частоты

**200 Гц возможны ТОЛЬКО через UART (проводное подключение).**

---

## 📊 Сравнение с немецким MicroSwing

**Немецкий MicroSwing:**
- Использует **проводное USB/Serial подключение**
- Работает на **50 Гц** (не 200 Гц!)
- Это оптимальная частота для их алгоритмов

**Наша система:**
- Использует **BLE** (беспроводное)
- Работает на **50 Гц** (как немецкое устройство)
- ✅ Полное соответствие!

**Вывод:** 50 Гц — это оптимальная частота для обеих систем. 200 Гц не нужны для данной задачи.
