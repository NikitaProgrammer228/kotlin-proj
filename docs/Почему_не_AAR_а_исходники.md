# Почему не получилось использовать AAR, а пришлось использовать исходный код

## 📦 Что такое AAR файл?

**AAR (Android Archive)** — это скомпилированная библиотека Android, похожая на JAR, но с дополнительными ресурсами.

### Особенности AAR:
- ✅ **Скомпилированный код** — уже скомпилирован в байт-код
- ✅ **Готов к использованию** — можно подключить одной строкой в `build.gradle`
- ❌ **Нельзя изменить** — исходный код недоступен, только скомпилированный `.class` файлы
- ❌ **Нельзя модифицировать** — нельзя добавить/изменить методы или логику

### Пример использования AAR:
```gradle
// В app/build.gradle
dependencies {
    implementation(name: 'wit-sdk', ext: 'aar')  // Подключаем AAR
}
```

---

## 🎯 Проблема: Нужно было увеличить частоту с 10 Гц до 50 Гц

### Исходная ситуация:
- Датчик WT901BLE выдавал данные с частотой **~10 Гц** (10 раз в секунду)
- Немецкий MicroSwing работает на **50 Гц** (50 раз в секунду)
- Для точного соответствия нужно было увеличить частоту в **5 раз**

### Причины низкой частоты (10 Гц):
1. **BLE Connection Interval** — Android по умолчанию использует интервал 30-100 мс между пакетами
2. **Перегрузка канала** — датчик отправлял все данные (acc + gyro + angle + mag), что забивало канал
3. **Отсутствие `requestConnectionPriority(HIGH)`** — не было запроса высокого приоритета соединения

---

## 🔧 Что нужно было изменить для достижения 50 Гц?

### Изменение 1: Добавить `requestConnectionPriority(HIGH)`

**Где:** В `CustomBluetoothBLE.java` или `BleConnectWorker.java`

**Что делает:**
- Уменьшает BLE connection interval с ~30-100 мс до ~7.5-15 мс
- Увеличивает пропускную способность BLE канала
- Позволяет получать данные с частотой 40-50 Гц вместо 10 Гц

**Код:**
```java
if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.LOLLIPOP) {
    boolean result = gatt.requestConnectionPriority(BluetoothGatt.CONNECTION_PRIORITY_HIGH);
    Log.i(TAG, "⚡ requestConnectionPriority(HIGH) = " + result);
}
```

### Изменение 2: Передать `BleConnectOptions` в `bluetoothkit`

**Где:** В `InukerBluetoothBLE.java`

**Проблема:** Исходный код вызывал `mClient.connect(mac, callback)` без опций:
```java
// ❌ БЫЛО (без опций):
mClient.connect(this.mac, (code, data) -> { ... });
```

**Решение:** Передать `BleConnectOptions`, чтобы `bluetoothkit` мог вызвать `requestConnectionPriority`:
```java
// ✅ СТАЛО (с опциями):
BleConnectOptions options = new BleConnectOptions.Builder()
    .setConnectRetry(1)
    .setConnectTimeout(10000)
    .build();
mClient.connect(this.mac, options, new BleConnectResponse() { ... });
```

**Почему это важно:** `bluetoothkit` автоматически вызывает `requestConnectionPriority(HIGH)` в `BleConnectWorker.java`, но только если переданы `BleConnectOptions`.

---

## ❌ Почему AAR не подходит?

### Проблема 1: Нельзя изменить скомпилированный код

**AAR содержит:**
- Скомпилированные `.class` файлы (байт-код)
- Ресурсы (layout, drawable и т.д.)
- Манифест

**Нет:**
- Исходного кода (`.java` файлов)
- Возможности модификации

**Аналогия:** Это как пытаться изменить готовый пирог — можно только съесть, но нельзя изменить рецепт.

### Проблема 2: Reflection не сработал

**Попытка:** Использовать reflection для получения `BluetoothGatt` из AAR:
```kotlin
// Пробовали через reflection:
val gatt = bluetoothBLE.javaClass.getDeclaredField("mBluetoothGatt")
gatt.requestConnectionPriority(BluetoothGatt.CONNECTION_PRIORITY_HIGH)
```

**Почему не сработало:**
- `BluetoothGatt` находится глубоко внутри `bluetoothkit`, а не в `wit-sdk`
- Структура классов в AAR не позволяет получить доступ к нужным полям
- Reflection ненадежен и может сломаться при обновлении SDK

### Проблема 3: Нельзя добавить новые методы

**Нужно было:**
- Изменить метод `connect()` в `InukerBluetoothBLE.java`
- Добавить передачу `BleConnectOptions`

**В AAR:**
- Метод `connect()` уже скомпилирован
- Нельзя переопределить или изменить его поведение

---

## ✅ Решение: Использовать исходный код как модуль

### Шаг 1: Найти исходный код SDK

**Где:** В папке `libs/Sample Codes/wit-opensdk-android/wit-sdk/`

**Структура:**
```
libs/Sample Codes/wit-opensdk-android/
└── wit-sdk/
    └── src/main/java/com/wit/witsdk/
        └── sensor/modular/connector/modular/bluetooth/
            ├── CustomBluetoothBLE.java
            ├── InukerBluetoothBLE.java
            └── BluetoothBLE.java
```

### Шаг 2: Добавить как модуль в `settings.gradle`

```gradle
include ':wit-sdk'
project(':wit-sdk').projectDir = file('libs/Sample Codes/wit-opensdk-android/wit-sdk')
```

### Шаг 3: Изменить зависимость в `app/build.gradle`

```gradle
// ❌ БЫЛО (AAR):
implementation(name: 'wit-sdk', ext: 'aar')

// ✅ СТАЛО (исходный код):
implementation project(':wit-sdk')
```

### Шаг 4: Внести изменения в исходный код

**Файл 1:** `InukerBluetoothBLE.java`
- Добавлен импорт `BleConnectOptions` и `BleConnectResponse`
- Изменен метод `connect()` для передачи опций

**Файл 2:** `BleConnectWorker.java` (в `bluetoothkit`)
- Уже содержал `requestConnectionPriority(HIGH)`, но не вызывался
- Добавлено логирование для отладки

---

## 🔄 Миграция с KAPT на KSP

### Проблема при использовании исходного кода:

**KAPT (Kotlin Annotation Processing Tool):**
- Старый способ обработки аннотаций
- Медленнее и менее надежен
- Проблемы с project dependencies

**Ошибка:**
```
e: Could not load module <Error module>
```

**Решение:** Переход на **KSP (Kotlin Symbol Processing)**
- Современная замена KAPT
- Лучше работает с project dependencies
- Быстрее и надежнее

**Изменения в `build.gradle`:**
```gradle
// ❌ БЫЛО:
plugins {
    id 'org.jetbrains.kotlin.kapt'
}
dependencies {
    kapt 'androidx.room:room-compiler:2.6.1'
}

// ✅ СТАЛО:
plugins {
    id 'com.google.devtools.ksp'
}
dependencies {
    ksp 'androidx.room:room-compiler:2.6.1'
}
```

---

## 📊 Сравнение: AAR vs Исходный код

| Параметр | AAR | Исходный код |
|----------|-----|--------------|
| **Простота подключения** | ✅ Одна строка | ⚠️ Нужно настроить модуль |
| **Возможность изменений** | ❌ Невозможно | ✅ Полный доступ |
| **Размер проекта** | ✅ Меньше | ⚠️ Больше (исходники) |
| **Время сборки** | ✅ Быстрее | ⚠️ Медленнее (компиляция) |
| **Отладка** | ⚠️ Сложнее | ✅ Легко (видно исходники) |
| **Обновление SDK** | ✅ Заменить файл | ⚠️ Нужно мержить изменения |

---

## 🎯 Итоговый результат

### Что получили:
- ✅ **50 Гц** вместо 10 Гц (увеличение в 5 раз)
- ✅ Полный контроль над кодом SDK
- ✅ Возможность дальнейших оптимизаций
- ✅ Легкая отладка (видно исходный код)

### Что потеряли:
- ⚠️ Немного больше времени на сборку
- ⚠️ Нужно вручную обновлять SDK (мержить изменения)

### Вывод:
**Для достижения 50 Гц было критически важно иметь доступ к исходному коду**, так как нужно было:
1. Добавить `requestConnectionPriority(HIGH)`
2. Изменить логику подключения в `InukerBluetoothBLE`
3. Обеспечить правильную передачу параметров в `bluetoothkit`

**AAR не позволяет этого сделать**, поэтому переход на исходный код был единственным решением.

---

## 🔍 Альтернативные подходы (и почему они не сработали)

### 1. Использовать reflection
- ❌ Ненадежно
- ❌ Может сломаться при обновлении
- ❌ Не дает доступ к внутренним полям

### 2. Создать обертку над AAR
- ❌ Не решает проблему — `requestConnectionPriority` нужно вызывать внутри SDK
- ❌ Нельзя изменить поведение `connect()`

### 3. Попросить производителя обновить AAR
- ⚠️ Долго (нужно ждать обновления)
- ⚠️ Может не включить нужные изменения
- ✅ Но это было бы идеальным решением для будущего

---

## 📝 Рекомендации на будущее

1. **Если нужны изменения в SDK:**
   - Используйте исходный код как модуль
   - Документируйте все изменения
   - Создайте патч-файлы для легкого обновления

2. **Если изменения не нужны:**
   - Используйте AAR (проще и быстрее)
   - Обновление — просто заменить файл

3. **Для продакшена:**
   - Рассмотрите возможность попросить производителя добавить `requestConnectionPriority` в официальный SDK
   - Это решит проблему для всех пользователей
