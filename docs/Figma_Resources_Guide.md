# Руководство по работе с ресурсами из Figma

## 📁 Структура папок для ресурсов

### Иконки и изображения
```
app/src/main/res/
├── drawable/              # Иконки и простые изображения
│   ├── ic_nav_home.png
│   ├── ic_nav_patients.png
│   ├── ic_nav_balance_test.png
│   ├── ic_nav_pkt_test.png
│   ├── ic_nav_settings.png
│   ├── ic_nav_exit.png
│   ├── ic_logo_simprove.png
│   └── ic_bluetooth_connected.xml (векторная)
├── drawable-hdpi/         # Для разных плотностей (опционально)
├── drawable-xhdpi/
├── drawable-xxhdpi/
└── mipmap-*/              # Только для иконки приложения
```

### Assets (для больших файлов или файлов, читаемых программно)
```
app/src/main/assets/
├── images/                # Большие изображения
│   └── screenshots/
└── fonts/                 # Кастомные шрифты (если нужны)
```

## 🎨 Как экспортировать из Figma

### 1. Иконки (лучший способ)

**Вариант A: PNG (для растровых иконок)**
1. Выделите иконку в Figma
2. Правый клик → "Copy as PNG" или "Export"
3. Выберите формат PNG
4. Экспортируйте в 1x, 2x, 3x (для разных плотностей экранов)
5. Сохраните в `app/src/main/res/drawable/` с именем `ic_название.png`

**Вариант B: SVG (для векторных иконок - предпочтительно)**
1. Выделите иконку
2. Правый клик → "Copy as SVG" или "Export" → SVG
3. Конвертируйте SVG в XML drawable (можно использовать онлайн-конвертер)
4. Сохраните как `ic_название.xml` в `app/src/main/res/drawable/`

**Вариант C: Прямо в assets (если нужны оригинальные файлы)**
1. Экспортируйте PNG/SVG
2. Сохраните в `app/src/main/assets/images/`
3. Загружайте программно через `AssetManager`

### 2. Цвета

1. Выделите элемент с нужным цветом
2. В панели справа скопируйте hex-код (например, `#E53935`)
3. Добавьте в `app/src/main/res/values/colors.xml`:
```xml
<color name="color_name">#E53935</color>
```

### 3. Размеры и отступы

1. Выделите элемент
2. В панели справа скопируйте значения:
   - Width, Height
   - Padding, Margin
   - Font size
3. Используйте в layout XML или передайте мне значения

### 4. Шрифты

1. Выделите текст
2. Скопируйте:
   - Font family (например, "Roboto", "Inter")
   - Font weight (Regular, Medium, Bold)
   - Font size (в sp для Android)
3. Если нужен кастомный шрифт - экспортируйте .ttf/.otf в `assets/fonts/`

## 📝 Именование ресурсов

### Иконки
- `ic_` - префикс для иконок
- `ic_nav_home` - иконка навигации "Домой"
- `ic_logo_simprove` - логотип SIMPROVE
- `ic_bluetooth_connected` - иконка Bluetooth

### Цвета
- `primary_red` - основной красный
- `background_light` - светлый фон
- `text_primary` - основной текст

### Изображения
- `img_` - префикс для изображений
- `img_background_pattern` - фоновый паттерн

## ✅ Рекомендации

1. **Используйте векторные иконки (SVG/XML)** - они масштабируются без потери качества
2. **Экспортируйте в нескольких размерах** для разных плотностей экранов (hdpi, xhdpi, xxhdpi)
3. **Сохраняйте оригиналы в assets** если нужно программно загружать
4. **Используйте осмысленные имена** файлов на английском
5. **Проверяйте размеры файлов** - большие изображения лучше оптимизировать

## 🔄 Процесс работы

1. Экспортируйте ресурсы из Figma
2. Сохраните в соответствующие папки
3. Пришлите мне список экспортированных файлов и их назначение
4. Я обновлю layout файлы для использования новых ресурсов
