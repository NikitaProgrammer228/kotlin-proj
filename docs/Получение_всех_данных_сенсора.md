# Получение всех данных с сенсора (ускорения, углы, гироскоп)

## 📊 Текущая ситуация

### Что мы получаем сейчас (RSW = ACC_ONLY = 0x02):

**Автоматические пакеты (50 Гц):**
- ✅ **Ускорения**: AccX, AccY, AccZ (в g)
- ❌ **Углы**: НЕ приходят автоматически
- ❌ **Гироскоп**: НЕ приходит автоматически
- ❌ **Магнитометр**: НЕ приходит автоматически

**Почему так:**
- Мы установили `RSW = 0x02` (только ускорение) для увеличения частоты до 50 Гц
- Это освобождает BLE канал от лишних данных
- Регистр RSW контролирует, что возвращается в **автоматических пакетах**

---

## 🔍 Что такое регистр RSW?

**RSW (Return Content Register, 0x02)** — контролирует, какие данные датчик отправляет автоматически:

| Бит | Значение | Данные |
|-----|----------|--------|
| 0x01 | RSW_TIME | Время |
| 0x02 | RSW_ACC | **Ускорения** (AccX, AccY, AccZ) |
| 0x04 | RSW_GYRO | Угловые скорости (GyroX, GyroY, GyroZ) |
| 0x08 | RSW_ANGLE | **Углы** (AngleX, AngleY, AngleZ) |
| 0x10 | RSW_MAG | Магнитометр (MagX, MagY, MagZ) |

**Комбинации:**
- `0x02` = только ускорения (текущая настройка)
- `0x0A` = ускорения + углы (0x02 + 0x08)
- `0x1E` = всё (0x02 + 0x04 + 0x08 + 0x10) — **по умолчанию, но даёт только 10 Гц**

---

## ✅ Можно ли получать все данные?

### Вариант 1: Чтение по запросу (текущий подход)

**Как работает:**
- Автоматические пакеты (50 Гц): только ускорения
- Углы и другие данные: читаем по запросу через команды чтения регистров

**Команда чтения регистра:**
```
FF AA 27 [регистр] 00
```

**Примеры:**
- `FF AA 27 51 00` — читать угол X (регистр 0x51)
- `FF AA 27 52 00` — читать угол Y (регистр 0x52)
- `FF AA 27 53 00` — читать угол Z (регистр 0x53)

**Преимущества:**
- ✅ Высокая частота ускорений (50 Гц)
- ✅ Можно получать углы по требованию
- ✅ Не перегружает BLE канал

**Недостатки:**
- ⚠️ Углы приходят реже (только по запросу)
- ⚠️ Нужно отправлять команды чтения

**Текущий код:**
```kotlin
// В BluetoothAccelerometerService.kt
val angleX = parseAngleDegrees(bwt901ble.getDeviceData(WitSensorKey.AngleX)) ?: 0.0
val angleY = parseAngleDegrees(bwt901ble.getDeviceData(WitSensorKey.AngleY)) ?: 0.0
```

**Проблема:** При RSW=ACC_ONLY углы могут быть `null`, так как они не приходят в автоматических пакетах.

---

### Вариант 2: Включить углы в автоматические пакеты

**Настройка:**
```kotlin
// RSW = ACC + ANGLE = 0x02 + 0x08 = 0x0A
val rswCommand = byteArrayOf(
    0xFF.toByte(), 0xAA.toByte(), 
    0x02,  // RSW register
    0x0A,  // RSW_ACC (0x02) + RSW_ANGLE (0x08)
    0x00
)
```

**Что получим:**
- ✅ Ускорения (50 Гц)
- ✅ Углы (50 Гц) — автоматически в каждом пакете
- ⚠️ Частота может упасть до ~30-40 Гц (больше данных в пакете)

**Преимущества:**
- ✅ Углы приходят автоматически с высокой частотой
- ✅ Не нужно отправлять команды чтения

**Недостатки:**
- ⚠️ Частота может быть ниже 50 Гц (но выше 10 Гц)
- ⚠️ Больше данных в каждом пакете

---

### Вариант 3: Включить всё (ACC + GYRO + ANGLE + MAG)

**Настройка:**
```kotlin
// RSW = всё = 0x1E (по умолчанию)
val rswCommand = byteArrayOf(
    0xFF.toByte(), 0xAA.toByte(), 
    0x02,  // RSW register
    0x1E,  // Всё (ACC + GYRO + ANGLE + MAG)
    0x00
)
```

**Что получим:**
- ✅ Все данные автоматически
- ❌ Частота упадёт до ~10 Гц (канал перегружен)

**Не рекомендуется** для нашей задачи (нужна высокая частота).

---

## 🎯 Рекомендация для вашего случая

### Для MicroSwing (как в немецком устройстве):

**Немецкий MicroSwing использует:**
- Только ускорения для расчёта позиции (двойная интеграция)
- Углы НЕ используются для основного алгоритма

**Наша текущая настройка (RSW=ACC_ONLY) — правильная:**
- ✅ 50 Гц ускорений (как нужно)
- ✅ Углы можно получать по запросу, если нужны для отображения/калибровки

### Если нужны углы с высокой частотой:

**Вариант:** RSW = ACC + ANGLE (0x0A)
- Ускорения: ~40-50 Гц
- Углы: ~40-50 Гц
- Компромисс между частотой и полнотой данных

---

## 💡 Реализация: Получение углов по запросу

### Текущая проблема:

В коде мы пытаемся читать углы:
```kotlin
val angleX = parseAngleDegrees(bwt901ble.getDeviceData(WitSensorKey.AngleX)) ?: 0.0
```

Но при RSW=ACC_ONLY они могут быть `null`, так как не приходят автоматически.

### Решение: Периодически запрашивать углы

**Добавить в `BluetoothAccelerometerService.kt`:**

```kotlin
// Запрашиваем углы раз в секунду (не перегружаем канал)
private var lastAngleRequestTime = 0L
private val ANGLE_REQUEST_INTERVAL_MS = 1000L  // Раз в секунду

override fun onRecord(bwt901ble: Bwt901ble) {
    // ... существующий код для ускорений ...
    
    // Запрашиваем углы периодически
    val now = System.currentTimeMillis()
    if (now - lastAngleRequestTime >= ANGLE_REQUEST_INTERVAL_MS) {
        lastAngleRequestTime = now
        requestAngles(bwt901ble)
    }
    
    // Пытаемся прочитать углы (могут быть null если ещё не пришли)
    val angleX = parseAngleDegrees(bwt901ble.getDeviceData(WitSensorKey.AngleX)) ?: 0.0
    val angleY = parseAngleDegrees(bwt901ble.getDeviceData(WitSensorKey.AngleY)) ?: 0.0
    // ...
}

private fun requestAngles(sensor: Bwt901ble) {
    // Читаем углы через команды чтения регистров
    // Регистры углов: 0x51 (X), 0x52 (Y), 0x53 (Z)
    sensor.sendProtocolData(byteArrayOf(0xFF.toByte(), 0xAA.toByte(), 0x27, 0x51, 0x00))
    Thread.sleep(10)  // Небольшая задержка между командами
    sensor.sendProtocolData(byteArrayOf(0xFF.toByte(), 0xAA.toByte(), 0x27, 0x52, 0x00))
    Thread.sleep(10)
    sensor.sendProtocolData(byteArrayOf(0xFF.toByte(), 0xAA.toByte(), 0x27, 0x53, 0x00))
}
```

---

## 📋 Сравнение вариантов

| Вариант | RSW | Ускорения | Углы | Гироскоп | Частота | Рекомендация |
|---------|-----|-----------|------|----------|---------|--------------|
| **Текущий** | 0x02 | ✅ 50 Гц | ⚠️ По запросу | ❌ | 50 Гц | ✅ **Для MicroSwing** |
| **ACC+ANGLE** | 0x0A | ✅ 40-50 Гц | ✅ 40-50 Гц | ❌ | 40-50 Гц | ⚠️ Если нужны углы часто |
| **Всё** | 0x1E | ⚠️ 10 Гц | ⚠️ 10 Гц | ⚠️ 10 Гц | 10 Гц | ❌ Не рекомендуется |

---

## 🎯 Итоговый ответ

### Да, можно получать все данные, но с компромиссами:

1. **Текущая настройка (RSW=ACC_ONLY):**
   - ✅ Ускорения: 50 Гц (автоматически)
   - ✅ Углы: можно получать по запросу (реже, но доступны)
   - ✅ Оптимально для MicroSwing алгоритма

2. **Если нужны углы с высокой частотой:**
   - Изменить RSW на `0x0A` (ACC + ANGLE)
   - Частота может быть 40-50 Гц (всё ещё хорошо)

3. **Если нужны все данные:**
   - Изменить RSW на `0x1E` (всё)
   - Но частота упадёт до 10 Гц (не рекомендуется)

**Для вашей задачи (MicroSwing):** Текущая настройка оптимальна — ускорения с высокой частотой, углы по запросу при необходимости.
