# Отладка Android-приложения

## Варианты отладки

### 1. 🔧 Android Studio (Рекомендуется)

**Лучший вариант для полноценной отладки:**

1. Откройте проект в Android Studio
2. Подключите устройство или запустите эмулятор
3. Нажмите `Shift + F9` или кнопку Debug
4. Установите breakpoints в коде
5. Используйте все возможности отладчика

**Преимущества:**
- ✅ Полноценный отладчик с breakpoints
- ✅ Просмотр переменных в реальном времени
- ✅ Step-by-step выполнение
- ✅ Hot reload
- ✅ Профилирование

### 2. 💻 Командная строка (из Cursor)

**Можно использовать для базовой отладки:**

#### Сборка и установка:
```bash
# Сборка APK
./gradlew assembleDebug

# Установка на устройство
./gradlew installDebug

# Или одной командой
./gradlew installDebug && adb shell am start -n com.accelerometer.app/.MainActivity
```

#### Просмотр логов:
```bash
# Все логи
adb logcat

# Только ваше приложение
adb logcat -s BluetoothAccelerometer:* AndroidRuntime:E

# Фильтр по тегу
adb logcat | grep "BluetoothAccelerometer"

# Очистить логи
adb logcat -c
```

#### Полезные команды:
```bash
# Список подключенных устройств
adb devices

# Перезапуск приложения
adb shell am force-stop com.accelerometer.app
adb shell am start -n com.accelerometer.app/.MainActivity

# Просмотр логов в реальном времени с фильтром
adb logcat -c && adb logcat | grep -E "(BluetoothAccelerometer|AccelerometerApp)"
```

### 3. 📝 Логирование в коде

Добавьте логи в ключевых местах:

```kotlin
import android.util.Log

class BluetoothAccelerometerService {
    companion object {
        private const val TAG = "BluetoothAccelerometer"
    }
    
    fun someMethod() {
        Log.d(TAG, "Debug message")
        Log.e(TAG, "Error message", exception)
        Log.w(TAG, "Warning message")
    }
}
```

### 4. 🔍 Удаленная отладка через ADB

Для подключения отладчика Java:

1. В `app/build.gradle` добавьте в `android.defaultConfig`:
```gradle
android {
    defaultConfig {
        // ... existing code ...
        testInstrumentationRunnerArguments clearPackageData: 'true'
    }
}
```

2. Запустите приложение с отладкой:
```bash
adb shell am start -D -n com.accelerometer.app/.MainActivity
```

3. Подключите отладчик (требует настройки JDWP)

## Использование задач VS Code/Cursor

В проекте есть файл `.vscode/tasks.json` с готовыми задачами:

1. **Build APK (Debug)** - собрать APK
2. **Install APK** - установить на устройство
3. **Run App** - установить и запустить
4. **View Logcat** - просмотреть логи
5. **Clear Logcat** - очистить логи

**Как использовать:**
- `Ctrl+Shift+P` (или `Cmd+Shift+P` на Mac)
- Введите "Tasks: Run Task"
- Выберите нужную задачу

## Рекомендуемый workflow

### Для разработки в Cursor:

1. **Редактируйте код в Cursor** ✏️
2. **Собирайте через терминал:**
   ```bash
   ./gradlew assembleDebug
   ```
3. **Устанавливайте на устройство:**
   ```bash
   ./gradlew installDebug
   ```
4. **Смотрите логи:**
   ```bash
   adb logcat -s BluetoothAccelerometer:* AndroidRuntime:E
   ```

### Для отладки с breakpoints:

1. **Откройте проект в Android Studio** 🚀
2. **Установите breakpoints**
3. **Запустите в режиме Debug**
4. **Используйте все возможности отладчика**

## Полезные команды ADB

```bash
# Проверка подключения устройства
adb devices

# Установка APK напрямую
adb install -r app/build/outputs/apk/debug/app-debug.apk

# Удаление приложения
adb uninstall com.accelerometer.app

# Просмотр информации о приложении
adb shell dumpsys package com.accelerometer.app

# Запуск Activity
adb shell am start -n com.accelerometer.app/.MainActivity

# Остановка приложения
adb shell am force-stop com.accelerometer.app

# Просмотр процессов
adb shell ps | grep accelerometer
```

## Отладка Bluetooth

Для отладки Bluetooth-подключения:

```bash
# Логи Bluetooth
adb logcat -s BluetoothAdapter:* BluetoothDevice:* BluetoothGatt:*

# Или все Bluetooth логи
adb logcat | grep -i bluetooth
```

## Советы

1. **Используйте логи** - добавьте `Log.d()` в ключевые места
2. **Проверяйте разрешения** - убедитесь, что все разрешения выданы
3. **Тестируйте на реальном устройстве** - Bluetooth на эмуляторе работает ограниченно
4. **Используйте Android Studio для сложной отладки** - breakpoints и step-by-step
5. **Cursor отлично подходит для написания кода** - используйте его для разработки

## Альтернатива: Гибридный подход

**Лучшая стратегия:**
- ✏️ **Cursor** - для написания и редактирования кода
- 🐛 **Android Studio** - для отладки, сборки и запуска
- 📱 **Терминал** - для быстрых операций (логи, установка)

Откройте проект в обоих редакторах одновременно - они не конфликтуют!

