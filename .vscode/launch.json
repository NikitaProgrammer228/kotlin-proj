{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Attach to Android Process",
            "type": "java",
            "request": "attach",
            "hostName": "localhost",
            "port": 5005,
            "processId": "${command:PickProcess}"
        }
    ]
}

